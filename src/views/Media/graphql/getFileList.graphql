query getFileList($contains: String) {
  getFileList(contains: $contains) {
    id
    createdAt
    description
    extension
    hasContent
    mimetype
    name
    owner
    size
    thumbnail
  }
}
